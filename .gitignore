node_modules/
dist/
*.tsbuildinfo
*.tgz

# Logs
*.log
npm-debug.log*
*.stackdump

# Coverage
coverage/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Environment
.env
.env.*
!.env.example
.npmrc
.codex/

# Claude
.claude/

# The knowledge graph a local tool writes from the sources.
/graphify-out/

# Where a change is worked, per AGENTS.md. Each holds a full checkout of this
# repository, which git and every linter would otherwise walk into.
/.worktrees/

# Local development server wiring (docs/DEVELOPMENT.md). The plugin's own
# plugins/codex-mcp/.mcp.json is tracked; only the repo-root one is local.
/.mcp.json
/.codex-mcp-state/

# Generated reference schemas (single-file outputs)
codex-app-server.schema.json
server-notifications.json
server-notifications.min.json
server-notifications.*.json

# rumdl cache (fallback when --no-config bypasses cache-dir)
.rumdl_cache/

# fallow analysis cache and churn history
.fallow/
